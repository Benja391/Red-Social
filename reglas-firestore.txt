rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Reglas para la colección de usuarios
    match /users/{userId} {
      // Los usuarios autenticados pueden leer cualquier perfil
      allow read: if request.auth != null;

      // Solo el usuario propietario del perfil puede modificarlo
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Reglas para la colección de publicaciones
    match /posts/{postId} {
      // Cualquier usuario autenticado puede leer publicaciones
      allow read: if request.auth != null;

      // Solo el creador de la publicación o un administrador puede modificarla o eliminarla
      allow write: if request.auth != null && 
                    (request.auth.uid == resource.data.userId || request.auth.token.admin == true);
    }

    // Reglas para la colección de comentarios
    match /comments/{commentId} {
      // Cualquier usuario autenticado puede leer comentarios
      allow read: if request.auth != null;

      // Solo el creador del comentario o un administrador puede modificarlo o eliminarlo
      allow write: if request.auth != null && 
                    (request.auth.uid == resource.data.userId || request.auth.token.admin == true);
    }
  }
}
